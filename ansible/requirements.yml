---
# Ansible Galaxy collection requirements for this repository's playbooks.
# Install on the control node with:
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: community.general
  - name: ansible.posix
